<UserControl x:Class="WpfAppProekt.Views.ProductFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:viewmodels="clr-namespace:WpfAppProekt.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.ColumnSpan="2" Text="{Binding Title}" FontWeight="Bold" Margin="0 0 0 12"/>
        <TextBlock Grid.Row="1" Text="Name:" Margin="0 0 8 0" VerticalAlignment="Center"/>
        <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding Model.Name, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Row="2" Text="Price:" Margin="0 8 8 0" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
            <TextBox Width="120" Text="{Binding Model.Price}"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="12 0 0 0" Width="110"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="8 0 0 0" Width="110"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="8 0 0 0" Width="110"/>

        </StackPanel>
    </Grid>
</UserControl>
